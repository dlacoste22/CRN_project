Algorithm1 et auxiliary_functions are two librairies needed for the identification of cores.

New_Bound_Randomize_Topo_and_Kinetics_delete=1 is a Jupyter notebook that generates codes with random stoichiometry and kinetics. Then it tests the bounds (fig 2 of the preprint) and the distribution of alpha (fig 3). It is also possible to test properties of a single network with it (function random network).

Alpha-distrib-only is the part of that code that deals with stoichiometry only and output the distribution of values of alpha. All the data is generated in folder result2 which contains the stoichiometric matrices of the generated random networks and their values of alpha.

Detect_Core_motifs list the cores which are contained in a specific realisation of a random network that can be chosen in line 7 of Entrée 7 (realization_id=5 par exemple)
The data is output is the folder named realization5 in this case.

A python version of the code is in the folder Minimum python-code, which I have run on the cluster

Generalized_Core_Eigen: is a note book to calculate the alpha, mu, eigenvalues and corresponding eigenvecotors of simpliest cores.

----------------------------------------------

Old codes:

Fin_ver: is an assembly of works for non-scalable networks before May.

Fixed_Topo_Calculate_alpha_and_mu: is for checking specific topology by both time evolution and solving steady state equations, its output include optimal flux, alpha, mu, long-term growth rate and steady state flow.

Fix_Topo_Randomize_Kinetics: is for sampling the kinetic space of fixed topology, its output include 2 figures -- 1. distribution of long-term growth rate and 2. shannon entropy of steady state vs long-term growth rate.

Randomize_Topo_and_Kinetics: is for studying the universality of stochiometric bounds via varied topologies and kinetics, its output include 3 figures: 1. alpha vs long-term growth rate, 2. catagorized stoichiometric bound vs long-term growth rate, 3. distribution of alpha among random generalized networks.

--------------------------------

Von-Neumann is a Jupyter of code to simulate Von Neumann economic model, it is from a book.

Simulate_single_network_from_data is to study a single network among an ensemble of random networks generated by other programs. Not working

Draw_distribution pour tracer les distributions de alpha générées par d'autres programmes.



  